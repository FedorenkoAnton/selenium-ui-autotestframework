package dev.selenium.pageelements;

import org.openqa.selenium.WebDriver;

public class Header extends BasicElement {

    public Header(String xPath, WebDriver driver) {
        super(xPath, driver);
    }
}
